# macOS-only runtime dependencies.
#
# Quartz CGEvent powers the Cmd+C synthesis used by highlight-to-speak;
# AppKit (NSWorkspace) gives us the frontmost app PID without needing
# Screen Recording permission; psutil walks the process tree;
# Speech is the SFSpeechRecognizer framework that drives the macOS
# voice-command recogniser (analogue of System.Speech on Windows) —
# triggers a one-time Speech Recognition permission prompt the first
# time the wake-word listener fires the recogniser. CoreAudio lets the
# mac mic watcher distinguish live external input from Terminal Talk output.
pyobjc-framework-Quartz>=10
pyobjc-framework-Cocoa>=12.1
pyobjc-framework-Speech>=10
pyobjc-framework-CoreAudio>=10
psutil>=5.9
