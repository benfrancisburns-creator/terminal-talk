# ACTIVE #2 — spinner-verb audio leaks into transcript narration

- **Status:** in-review
- **Owner:** tt1
- **Axes in play:** 1 (correctness), 6 (observability)
- **MAP page:** [`MAP/heartbeat-narration.md`](../MAP/heartbeat-narration.md)
- **Reported by:** Ben, live use, 2026-04-24

## User-reported symptom

> "Tinkering, you can actually hear the words in this transcript now so we need to fix that"
>
> — i.e. spinner verbs that should be quiet/ephemeral heartbeat audio are being heard at
> full-body-clip volume (and/or during turns when heartbeat is supposed to be suppressed),
> to the point where they sound like they're part of the transcript narration itself.

## Reproduction recipe — [tt1 · 2026-04-24T21:50:00+01:00]

### Recipe A — trigger the "heartbeat during Claude-thinking" window

```
# PREP
1. Confirm heartbeat is enabled in config (workaround for #1 which drops the key):
      Open Settings, toggle heartbeat OFF then back ON. Close.
      (Bug #1 means toggling writes nothing, but ON is the current default so CFG is ON.)
2. Confirm queue is empty:
      ls ~/.terminal-talk/queue/*.mp3

# TRIGGER
3. In a Claude Code terminal, submit a prompt that will take >10 s to produce first
   response token (a reasoning-heavy prompt, e.g. "Count to 20 in Spanish with commentary").
4. LISTEN — within ~5-12 s you'll hear one heartbeat clip play: a verb like "Moonwalking" or
   "Tinkering" at ~45% volume, ~1-2 s long.
5. THEN the actual transcript clips start.

# OBSERVE
6. Is the heartbeat clip AUDIBLY different from the body clips (quieter, clearly background)?
   OR does it sound like it's part of the narration flow (similar volume + cadence)?
7. Open ~/.terminal-talk/queue/ and confirm filenames:
   - Heartbeat clip: pattern `<ts>-H-0001-<short>.mp3` (H after the timestamp)
   - Body clips:    pattern `<ts>-0000-<short>.mp3` and `<ts>-0001-<short>.mp3` (no H)
```

### Recipe B — trigger the "end-of-turn footer verb" path (past-tense)

```
# PREP
1. Launch toolbar. Ensure a clean queue.

# TRIGGER
2. In Claude Code, submit any prompt that takes >60 s (long tool-call sequence works).
3. Let it finish. After the final response, LISTEN for a closing clip like:
   "Tinkered for 1 minute and 23 seconds"  OR
   "Sautéed for 90 seconds"  OR
   "Pondered for 2 minutes and 5 seconds"

# OBSERVE
4. This is synth_turn.py's format_elapsed_phrase — a BODY clip (not H-prefixed), plays at
   full volume. Present in the queue as `<ts>-NNNN-<short>.mp3`.
   (The verb is PAST tense: "Tinkered", "Moonwalked", etc. If Ben heard "Tinkering"
   present-tense, it's Recipe A not B. If "Tinkered" past-tense, it's Recipe B.)
```

### Why two recipes

The two paths produce AURALLY similar audio (same voice, spinner-feel verbs) but through
completely different code. Discriminating them empirically is the only way to know which fix
to apply. Ben's quoted "Tinkering" could be either tense; a careful listen on real audio is
required.

## Empirical evidence already gathered — [tt1 · 2026-04-24T21:50:00+01:00]

`ls ~/.terminal-talk/queue/ | head`:

```
20260424T213257254-0000-a29f747b.mp3   ← body clip (pre-#2 reporting window)
20260424T213435237-0000-a29f747b.mp3   ← body clip
20260424T213519483-H-0001-a29f747b.mp3  ← heartbeat clip for session a29f747b
```

The H-file's filename was verified against `isHeartbeatClip` regex
(`/-H-\d{4}-[a-f0-9]{8}\.(wav|mp3)$/i`):

```
filename: 20260424T213519483-H-0001-a29f747b.mp3
matches isHeartbeatClip: true
matches isEphemeralClip: true
```

**Classification is not broken on Ben's install.** Any heartbeat clip generated by the current
`speak-heartbeat` IPC gets the H-prefix, matches the regex, plays at 0.45 × master volume, and
auto-deletes on play-end. The "leak" is NOT a mis-classification.

## Reviewer findings — [tt1 · 2026-04-24T21:50:00+01:00]

### Code-path map — two distinct paths produce spinner-verb-style audio

**Path A — heartbeat verb (present-continuous):**

1. Renderer tick fires at POLL_INTERVAL_MS, reads `workingSessionsCache`, calls
   `decideHeartbeatAction` (`app/lib/heartbeat.js:120-153`).
2. If conditions met → picks verb via `pickHeartbeatVerb` (`heartbeat.js:89-95`). Returns
   either a `SPINNER_VERBS` entry (60%) or a `THINKING_PHRASES` entry (40%). SPINNER_VERBS
   includes `'Tinkering'`, `'Moonwalking'`, `'Flibbertigibbeting'`, etc.
3. Renderer calls `window.api.speakHeartbeat(verb, sessionShort)` (`renderer.js:299-300`).
4. IPC handler `speak-heartbeat` (`app/lib/ipc-handlers.js:584-626`):
   - Validates verb `/^[A-Za-z][A-Za-z ]{1,59}$/`
   - Validates sessionShort `/^[a-f0-9]{8}$/`
   - Writes filename `${ts}-H-0001-${sessionShort}.mp3` (line 614)
   - Calls `callEdgeTTS(verb, voice, outPath)` — ALWAYS edge-tts, NEVER OpenAI (no provider
     branch in this handler — heartbeat is free-TTS-only by design)
5. Clip appears in `~/.terminal-talk/queue/` with H-prefix.
6. Queue-watcher picks it up, adds to renderer's `queue` array.
7. `playNextPending` picks next clip by priority/focus/mtime; heartbeat clips compete with body
   clips as plain queue entries.
8. `audioPlayer.playPath` reads filename, runs `isHeartbeatClip(fn)` → true → volume
   `0.45 × masterVolume` (`audio-player.js:278-288`).
9. After play-end, `onClipEnded` + `scheduleAutoDelete` tear down the clip (ephemeral, quick
   auto-delete; not subject to the body auto_prune_sec delay).

**Path B — end-of-turn footer verb (past-tense):**

1. Stop hook fires. `hooks/speak-response.ps1` computes `elapsedSec` and attempts UIA scrape for
   the terminal's own footer text.
2. If scrape succeeds → uses scraped phrase. If fails → `synth_turn.py` falls back to
   `format_elapsed_phrase(elapsed_sec)` (`synth_turn.py:1214-1244`).
3. `format_elapsed_phrase` picks a random verb from **`PAST_TENSE_VERBS`** (`synth_turn.py:
   1194-1211`) — a tuple containing `'Tinkered'`, `'Moonwalked'`, `'Sautéed'`, `'Flibbertigibbeted'`,
   `'Pondered'`, etc. Formats as `"<Verb> for N second(s)"` or `"<Verb> for M minute(s) and N
   second(s)"`.
4. This string gets **APPENDED as a body clip** — no H-prefix, no T-prefix, full body volume,
   subject to auto_prune_sec like any other body clip.
5. Plays as the final clip of the turn at 1.0 × master volume.

### State surfaces that hold "spinner verbs"

| # | Surface | File · line | Tense | Classified as |
|---|---|---|---|---|
| V1 | `SPINNER_VERBS` | `app/lib/heartbeat.js:44-60` | present-continuous | heartbeat (H-prefix, 0.45 vol, ephemeral) |
| V2 | `THINKING_PHRASES` | `app/lib/heartbeat.js:66-81` | contextual phrases | heartbeat (same) |
| V3 | `PAST_TENSE_VERBS` | `app/synth_turn.py:1194-1211` | past tense | **body clip** (no prefix, 1.0 vol, persistent) |
| V4 | Terminal-scraped footer phrase | `app/terminal-scrape.psm1` + `hooks/speak-response.ps1` | as-rendered by Claude Code (usually past) | body clip |

### Hypotheses ranked

| # | Hypothesis | Verdict | Evidence / what would confirm |
|---|---|---|---|
| **H1** | Ben heard the **PAST-tense `format_elapsed_phrase` footer** ("Tinkered for X seconds") — this IS a body clip by design, plays at full volume, sounds like part of the transcript because it IS appended to the transcript | **LIVE — most likely** | Ben's word "Tinkering" could be an approximate recall of "Tinkered". Recipe B isolates. If he hears "Tinkered for N seconds" at turn-end at full volume, this is the hypothesis. Fix = either suppress the footer clip entirely (opt-in setting), or mark it ephemeral, or give it a distinct voice-change. |
| **H2** | Temporal leak — heartbeat fires during the "Claude is thinking" silent window BEFORE the first body clip lands. Heartbeat clip sits in queue with older mtime than body clips. When playback starts, heartbeat plays FIRST (at 0.45 vol), then body clips play after. User perceives the sequence as "a weird verb, then my transcript" = leak. | **LIVE — second most likely** | Ben's queue currently has evidence of heartbeat and body clips coexisting. Recipe A isolates. If he hears a present-continuous verb (`"Tinkering"`, `"Moonwalking"`) at ~45% volume BEFORE the transcript proper starts, this is the hypothesis. Fix = suppress heartbeat for some grace window once a body clip lands for the same session, OR use a prefix chime so user recognises "this is heartbeat, not transcript". |
| H3 | Heartbeat volume at 0.45 × master is too loud relative to body — user perceives any audible heartbeat as "transcript-adjacent" | Supporting factor for H2 but not the root cause | Fix would be tuning: drop to 0.25 or similar. But H2's clip-placement issue remains even at lower volume. |
| H4 | Classification regex misfires — H-prefix clip plays at body volume | **RULED OUT** | Empirically: Ben's live H-file matches `isHeartbeatClip` regex (verified via `node -e` one-liner above). |
| H5 | `speak-heartbeat` IPC routes through OpenAI TTS because Ben's provider = openai — different voice, different cadence, different perception | **RULED OUT** | `ipc-handlers.js:599` — `speak-heartbeat` calls `callEdgeTTS` unconditionally, no provider branch. OpenAI path never touches heartbeat clips. |
| H6 | Present-continuous verb text ("Tinkering") leaks into body transcript content via a sanitiser bug | **RULED OUT** | `rg -i 'spinner|moonwalk|tinker' app/synth_turn.py` returns only the V3 list of PAST-tense verbs + comments referencing V1 for documentation. No code path concatenates V1 or V2 into body clip text. |

### Most likely scenario to check first

Given Ben's specific word "**Tinkering**", H1 is the first to test because:

- `PAST_TENSE_VERBS` at `synth_turn.py:1208` contains `'Tinkered'`. Spoken by edge-tts, at
  normal UK voice cadence, the -ed suffix becomes -t or -id depending on the voice model.
  "Tinkered" heard quickly can phonetically register as "Tinkering" to the listener. "Sautéed"
  lands as "so-tayed" which sounds verbing-like too.
- It's a BODY clip by design, plays at full volume, sits on the dot strip until auto-prune —
  matches Ben's description "leaking into real transcript" more closely than the quiet
  heartbeat path.
- There's a Ben-specific comment at `synth_turn.py:1190-1191`: *"Sautéed explicitly included
  because the terminal uses it and it's what Ben pointed at in the transcript."* Indicates
  this clip has previously confused Ben — the V3 list was CHOSEN to mirror Claude Code's own
  verb set, but the aural effect is the same verb-style as heartbeats.

**If TT2's empirical test confirms H1:** fix is design-level. Options to put in front of Ben:
  1. Opt-out setting (`playback.speak_elapsed_phrase: false`) — hide the footer entirely for
     users who find it intrusive.
  2. Voice contrast — synthesise the footer clip with a different edge-tts voice so it reads
     as a distinct "meta" layer (like `en-GB-LibbyNeural` vs `en-GB-RyanNeural` for the body).
  3. Volume reduction — tag the footer clip with a new prefix (`-F-` for footer?) and add a
     classifier rule so it plays at, say, 0.6 × master.

**If H2 is confirmed instead (or additionally):** fix is a gate in `decideHeartbeatAction` —
when `workingSessionsCache` is non-empty AND a body-clip for that session has landed within
the last N seconds, skip heartbeat emission. Or simpler: track a per-session `firstBodyClipAt`
and forbid heartbeat until N seconds *after* the turn's first body clip has played out (letting
heartbeat fire ONLY in the pre-first-audio window, not during gaps between body clips).

### Pattern note (adjacent risk, not bug)

Both V1 and V3 lists are maintained separately and can drift. Adding a verb to `SPINNER_VERBS`
but not `PAST_TENSE_VERBS` (or vice-versa) doesn't break functionality but creates aural
inconsistency. **Out of scope for #2** but worth flagging for a future QUEUE item: "unify
spinner-verb lexicon into a single source with morphology function".

## Tester findings — [TT? · HH:MM]

*(TT2 runs Recipe A and Recipe B, reports which produces the audible "leak". Key data:
  - For each trigger, what is the EXACT English word Ben/listener hears (e.g. "Tinkering",
    "Tinkered", "Moonwalking", "Moonwalked for 20 seconds").
  - What is the filename of the offending clip in `~/.terminal-talk/queue/` immediately after
    playback — specifically whether it has `-H-` or not.
  - Does the offending clip play BEFORE the transcript (H2/Recipe A) or AFTER (H1/Recipe B)?
  - What's the master volume slider set to?)*

## Tester findings — [TT? · HH:MM]

*(TT2 fills in after reviewer ships. Listen for the specific failure mode; capture the clip
filenames from `~/.terminal-talk/queue/` during a turn where heartbeat fires, check whether they
match the `-H-\d{4}-[a-f0-9]{8}\.(wav|mp3)$` classifier regex.)*

## Root-cause diagnosis — [TT? · HH:MM]

*(Whoever finds it writes a paragraph: what path produces an audible spinner-verb clip that is
NOT quiet-and-ephemeral.)*

## Fix proposal — [TT? · HH:MM]

*(Code diff / PR link.)*

## Blast-radius check — [TT? · HH:MM]

- **Files touched:** *(to be filled)*
- **Features depending on those files (from MAP):** *(to be filled)*
- **Invariants spanning those files (from INDEX):** *(to be filled)*
- **Tests that MUST still pass:** *(to be filled)*
- **Tests at silent-regression risk:** *(to be filled)*
- **Settings / flag files possibly affected:** *(to be filled)*

## Causality

- **Root cause:** *(not symptom)*
- **How did this escape prior review?**
- **Is the fix addressing the cause or the symptom?**
- **Smallest fix that addresses the cause:**

## Devil's advocate — [OTHER TERMINAL · HH:MM]

*(The terminal that DID NOT draft the fix fills this. What could this change break that the
author didn't consider? If we ship this, what's the first report we'd expect from a user?)*

## New test that guards against regression

*(Required before close — file path + test name.)*

## Verification — [TT2 · HH:MM]

*(TT2 runs the fix against the original recipe in live install. Reports: bug reproduced pre-fix,
bug NOT reproduced post-fix, other features unaffected.)*

## Close-out checklist

- [ ] Bug reproduced on pre-fix install
- [ ] Fix applied; bug no longer reproduces
- [ ] New test in place and green
- [ ] `MAP/heartbeat-narration.md` updated with invariants discovered + files touched
- [ ] `INDEX.md` updated — new invariant row, new historical-bugs row
- [ ] Commit + push
- [ ] Move this file to `DONE/2-tinkering-audio-leak.md`
- [ ] Update `QUEUE.md` — #2 STATUS=done
