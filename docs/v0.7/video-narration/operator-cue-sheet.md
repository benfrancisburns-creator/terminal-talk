# Operator Cue Sheet

This is the live prompt sheet for the two-screen Terminal Talk product proof.

Use these prompts one at a time. Wait for the UI state to match the cue before sending the prompt.

## Registration

Send to `Claude TT A` after it appears in the registry:

```text
Read docs/video-narration/claude-tt-a.md and output cue CLAUDE-A-01 only.
```

Send to `Codex TT A` as the first Codex prompt:

```text
Read docs/video-narration/codex-tt-a.md and output cue CODEX-A-01 only.
```

Send to `Claude TT B` after it appears in the registry:

```text
Read docs/video-narration/claude-tt-b.md and output cue CLAUDE-B-01 only.
```

Send to `Codex TT B` as the first Codex B prompt:

```text
Read docs/video-narration/codex-tt-b.md and output cue CODEX-B-01 only.
```

## Queue And Identity

Send these while the toolbar is expanded and the dot strip is visible.

```text
Read docs/video-narration/claude-tt-a.md and output cue CLAUDE-A-04 only.
```

```text
Read docs/video-narration/codex-tt-a.md and output cue CODEX-A-04 only.
```

```text
Read docs/video-narration/claude-tt-b.md and output cue CLAUDE-B-04 only.
```

```text
Read docs/video-narration/codex-tt-b.md and output cue CODEX-B-04 only.
```

## Transcript

Open the transcript panel first. Then send:

```text
Read docs/video-narration/claude-tt-a.md and output cue CLAUDE-A-05 only.
```

```text
Read docs/video-narration/codex-tt-a.md and output cue CODEX-A-05 only.
```

## Collapsed Toolbar

Let the toolbar collapse before sending each cue.

```text
Read docs/video-narration/claude-tt-a.md and output cue CLAUDE-A-06 only.
```

```text
Read docs/video-narration/codex-tt-a.md and output cue CODEX-A-06 only.
```

```text
Read docs/video-narration/claude-tt-b.md and output cue CLAUDE-B-06 only.
```

```text
Read docs/video-narration/codex-tt-b.md and output cue CODEX-B-06 only.
```

## Closing Line

Use this at the end with all four sessions visible.

```text
Read docs/video-narration/codex-tt-b.md and output cue CODEX-B-07 only.
```
